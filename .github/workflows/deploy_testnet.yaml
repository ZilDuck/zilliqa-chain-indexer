name: Deploy testnet

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.16.0'
      - name: Inject Dependencies
        run: |
          ./bin/di
      - name: Install vendors
        run: |
          go version
          go mod vendor

      - name: Copy Mappings
        run: |
          mkdir -p ./build
          cp -r config ./build/config

      - name: Copy Restarter
        run: |
          mkdir -p ./build/bin
          cp bin/restart ./build/bin/restart

      - name: Build Indexerd
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/indexerd ./cmd/indexerd/main.go

      - name: Build Block Importer
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/blockImporter ./cmd/importBlock/main.go

      - name: Build Contract Importer
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/contractImporter ./cmd/importContracts/main.go

      - name: Build NFT Importer
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/nftImporter ./cmd/importNfts/main.go

      - name: Queue Subscriber
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/queueSubscriber ./cmd/queueSubscriber/main.go

      - name: Build Update Contract Assets
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/loadAssets ./cmd/loadAssets/main.go

      - name: Serve NFT Assets
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/serveAsset ./cmd/serveAsset/main.go

      - name: Missing TXs
        run: GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./build/missingTxs ./cmd/missingTxs/main.go

      - name: Deploy to Hetzner
        uses: easingthemes/ssh-deploy@v2.1.5
        env:
          SSH_PRIVATE_KEY: ${{ secrets.ZILKROAD_HETZNER_INDEXER }}
          REMOTE_HOST: ${{ secrets.ZILKROAD_INDEXER_HOST }}
          REMOTE_USER: ${{ secrets.ZILKROAD_INDEXER_USER }}
          SOURCE: "./build/*"
          TARGET: "/app/indexer/testnet"

      - name: Restart indexer
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.ZILKROAD_INDEXER_HOST }}
          username: ${{ secrets.ZILKROAD_INDEXER_USER }}
          key: ${{ secrets.ZILKROAD_HETZNER_INDEXER }}
          port: 22
          script: "/app/indexer/testnet/bin/restart testnet"
